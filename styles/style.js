export const videoStyle = {
  height: "1920px",
  position: "absolute",
  top: 0,
  transform: 'translateX(calc(-50% + 540px))',
}

const videoWidth = 1080
const videoHeight = 1920

export const bgGradientStyle = {
  width: videoWidth * 5,
  height: videoHeight * 5,
}